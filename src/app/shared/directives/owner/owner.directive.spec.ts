import { OwnerDirective } from './owner.directive';

describe('OwnerDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnerDirective();
    expect(directive).toBeTruthy();
  });
});
