import { LoggedDirective } from './logged.directive';

describe('LoggedDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggedDirective();
    expect(directive).toBeTruthy();
  });
});
