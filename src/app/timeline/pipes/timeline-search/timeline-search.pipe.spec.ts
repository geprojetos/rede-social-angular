import { TimelineSearchPipe } from './timeline-search.pipe';

describe('TimelineSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
