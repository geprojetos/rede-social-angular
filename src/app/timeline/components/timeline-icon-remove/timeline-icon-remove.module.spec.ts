import { TimelineIconRemoveModule } from './timeline-icon-remove.module';

describe('TimelineIconRemoveModule', () => {
  let timelineIconRemoveModule: TimelineIconRemoveModule;

  beforeEach(() => {
    timelineIconRemoveModule = new TimelineIconRemoveModule();
  });

  it('should create an instance', () => {
    expect(timelineIconRemoveModule).toBeTruthy();
  });
});
